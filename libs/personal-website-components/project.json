{
  "name": "personal-website-components",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/personal-website-components/src",
  "prefix": "lib",
  "projectType": "library",
  "tags": ["ui"],
  "targets": {
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "reportsDirectory": "../../coverage/libs/personal-website-components"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
